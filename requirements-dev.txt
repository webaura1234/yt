# api/requirements.txt already pulls in requirements.txt; including it here too
# means `pip install -r requirements-dev.txt` covers the dashboard API, whose
# tests (tests/test_api_*.py) import fastapi.
-r api/requirements.txt
pytest==8.3.4
ruff==0.8.4
